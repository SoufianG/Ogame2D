export interface Resources {
  metal: number;
  crystal: number;
  deuterium: number;
}

export interface ResourceProduction {
  metal: number;
  crystal: number;
  deuterium: number;
  energy: number;
}

export interface ResourceStorage {
  metal: number;
  crystal: number;
  deuterium: number;
}
